import { Team } from '../../types/Team';

const oneTeamMock: Team = { id: 1, teamName: 'Avaí/Kindermann' };

const arrayTeamsMock: Team[] = [oneTeamMock];


export {
  oneTeamMock,
  arrayTeamsMock,
}